---
phase: 05-meeting-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/intelligence/__init__.py
  - engine/intelligence/meeting_classifier.py
  - engine/context/models.py
autonomous: true

must_haves:
  truths:
    - "Meetings with all @aiacrobatics.com attendees classified as internal"
    - "Meetings with any non-aiacrobatics.com attendee classified as client"
    - "Meeting type available in UnifiedMeetingContext for downstream consumers"
  artifacts:
    - path: "engine/intelligence/__init__.py"
      provides: "Intelligence module package init"
    - path: "engine/intelligence/meeting_classifier.py"
      provides: "classify_meeting_type function"
      exports: ["classify_meeting_type", "MeetingType"]
    - path: "engine/context/models.py"
      provides: "MeetingType field on UnifiedMeetingContext"
      contains: "meeting_type"
  key_links:
    - from: "engine/intelligence/meeting_classifier.py"
      to: "engine/context/models.py"
      via: "MeetingType enum import"
      pattern: "MeetingType"
    - from: "engine/intelligence/meeting_classifier.py"
      to: "AttendeeIdentity.email"
      via: "domain extraction from attendee emails"
      pattern: "email.*split.*@"
---

<objective>
Create internal vs client meeting classifier based on attendee email domains.

Purpose: Meetings with only @aiacrobatics.com attendees are internal (can reference all projects, casual tone). Meetings with external domains are client meetings (scoped to their project, professional tone). This classification drives downstream behavior: prior context surfacing scope, follow-up email tone, and project routing.

Output: MeetingType enum and classify_meeting_type function, integrated into UnifiedMeetingContext.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@engine/context/models.py
@engine/context/contacts.py
@engine/context/assembler.py
@engine/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meeting classifier module</name>
  <files>engine/intelligence/__init__.py, engine/intelligence/meeting_classifier.py</files>
  <action>
    Create `engine/intelligence/__init__.py` as empty package init.

    Create `engine/intelligence/meeting_classifier.py` with:

    1. `MeetingType` enum (str, Enum): `INTERNAL = "internal"`, `CLIENT = "client"`, `UNKNOWN = "unknown"`

    2. `INTERNAL_DOMAINS` set: `{"aiacrobatics.com"}` — configurable, could add more later.

    3. `classify_meeting_type(attendee_emails: list[str]) -> MeetingType` function:
       - Extract domain from each email (split on @, lowercase)
       - Filter out Julian's own email (julianb233@gmail.com, julian@aiacrobatics.com) — the owner is always present, should not count
       - If no external attendees remain after filtering owner: return UNKNOWN (solo meeting or no attendees)
       - If ALL remaining attendee domains are in INTERNAL_DOMAINS: return INTERNAL
       - If ANY remaining attendee domain is NOT in INTERNAL_DOMAINS: return CLIENT
       - Edge case: empty list returns UNKNOWN

    4. `get_client_domains(attendee_emails: list[str]) -> set[str]` helper:
       - Returns the set of non-internal, non-owner domains from attendee emails
       - Useful downstream for scoping context to the client's company

    Use standard logging. Add a `if __name__ == "__main__"` test block with sample emails to verify classification.
  </action>
  <verify>
    Run: `cd /opt/agency-workspace/meeting-copilot/engine && python -c "
from intelligence.meeting_classifier import classify_meeting_type, MeetingType
# Internal meeting
assert classify_meeting_type(['julian@aiacrobatics.com', 'hitesh@aiacrobatics.com']) == MeetingType.INTERNAL
# Client meeting
assert classify_meeting_type(['julian@aiacrobatics.com', 'sean@hafniafin.com']) == MeetingType.CLIENT
# Solo/empty
assert classify_meeting_type(['julian@aiacrobatics.com']) == MeetingType.UNKNOWN
assert classify_meeting_type([]) == MeetingType.UNKNOWN
print('All assertions passed')
"`
  </verify>
  <done>classify_meeting_type correctly identifies internal, client, and unknown meeting types based on attendee email domains</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MeetingType into UnifiedMeetingContext</name>
  <files>engine/context/models.py, engine/context/assembler.py</files>
  <action>
    1. In `engine/context/models.py`:
       - Import MeetingType from `intelligence.meeting_classifier`
       - Add `meeting_type: str = "unknown"` field to `UnifiedMeetingContext` (use str not enum for JSON serialization simplicity, matching existing pattern)
       - Add `client_domains: list[str] = Field(default_factory=list)` field to UnifiedMeetingContext
       - In `to_classifier_prompt()`, add a line at the top after meeting title: `"Meeting type: {self.meeting_type} ({', '.join(self.client_domains) if self.client_domains else 'N/A'})"`

    2. In `engine/context/assembler.py`:
       - Import `classify_meeting_type` and `get_client_domains` from `intelligence.meeting_classifier`
       - After building the attendee_contexts list (just before the return statement), call:
         ```
         meeting_type = classify_meeting_type(emails)
         client_domains = get_client_domains(emails)
         ```
       - Pass `meeting_type=meeting_type.value` and `client_domains=sorted(client_domains)` to the UnifiedMeetingContext constructor

    Keep all existing fields and behavior unchanged. The meeting_type is purely additive.
  </action>
  <verify>
    Run: `cd /opt/agency-workspace/meeting-copilot/engine && python -c "
import asyncio
from context.assembler import assemble_meeting_context
ctx = asyncio.run(assemble_meeting_context(['julian@aiacrobatics.com', 'hitesh@aiacrobatics.com'], meeting_title='Standup'))
print('meeting_type:', ctx.meeting_type)
assert ctx.meeting_type == 'internal', f'Expected internal, got {ctx.meeting_type}'
prompt = ctx.to_classifier_prompt()
assert 'Meeting type: internal' in prompt
print('Integration verified')
"`
  </verify>
  <done>UnifiedMeetingContext includes meeting_type and client_domains fields, populated by the assembler, and surfaced in the classifier prompt</done>
</task>

</tasks>

<verification>
- `python -c "from intelligence.meeting_classifier import classify_meeting_type, MeetingType; print('import ok')"` succeeds from engine directory
- MeetingType correctly classifies internal (@aiacrobatics.com only) vs client (external domains present)
- UnifiedMeetingContext.meeting_type is populated by assembler
- to_classifier_prompt() includes meeting type information
- No existing tests or functionality broken
</verification>

<success_criteria>
- Meetings with all @aiacrobatics.com attendees return meeting_type="internal"
- Meetings with any external attendee return meeting_type="client"
- Solo meetings or empty attendee lists return meeting_type="unknown"
- The classifier prompt includes meeting type for downstream LLM consumption
</success_criteria>

<output>
After completion, create `.planning/phases/05-meeting-intelligence/05-01-SUMMARY.md`
</output>
