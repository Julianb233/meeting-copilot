---
phase: 04-zoom-panel
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - panel/src/components/QuickActionButton.tsx
  - panel/src/components/QuickActions.tsx
  - panel/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Quick action buttons for Delegate Task, Create Proposal, Research This, Draft Email, Check Domain are visible in panel footer"
    - "Clicking a quick action sends a typed WebSocket message via sendAction"
    - "Buttons are disabled when engine is disconnected"
    - "Buttons show loading spinner after click with 5-second fallback timeout"
    - "App.tsx renders PanelLayout with useZoomContext and useEngine wired in"
    - "Panel builds successfully with `npm run build`"
  artifacts:
    - path: "panel/src/components/QuickActionButton.tsx"
      provides: "Individual action button with loading state"
      exports: ["QuickActionButton"]
    - path: "panel/src/components/QuickActions.tsx"
      provides: "Grid of all 5 quick action buttons"
      exports: ["QuickActions"]
    - path: "panel/src/App.tsx"
      provides: "Root component wiring PanelLayout + useZoomContext + useEngine"
      exports: ["default"]
  key_links:
    - from: "panel/src/components/QuickActions.tsx"
      to: "panel/src/hooks/useEngine.ts"
      via: "sendAction callback for WebSocket dispatch"
      pattern: "sendAction"
    - from: "panel/src/components/QuickActions.tsx"
      to: "panel/src/types/messages.ts"
      via: "QuickActionType for typed action dispatch"
      pattern: "QuickActionType"
    - from: "panel/src/App.tsx"
      to: "panel/src/components/PanelLayout.tsx"
      via: "renders PanelLayout as main content"
      pattern: "PanelLayout"
    - from: "panel/src/App.tsx"
      to: "panel/src/hooks/useZoomContext.ts"
      via: "useZoomContext for Zoom status"
      pattern: "useZoomContext"
---

<objective>
Create quick action buttons and wire the complete panel together by refactoring App.tsx to use PanelLayout with useZoomContext.

Purpose: Quick action buttons are the primary way Julian triggers fleet agents during meetings. This plan completes the panel UI by adding the action bar and replacing the flat App.tsx with the proper component hierarchy from Plans 01 and 02.

Output: QuickActionButton, QuickActions components, refactored App.tsx, passing build
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-zoom-panel/04-RESEARCH.md
@.planning/phases/04-zoom-panel/04-01-SUMMARY.md
@.planning/phases/04-zoom-panel/04-02-SUMMARY.md
@panel/src/App.tsx
@panel/src/hooks/useEngine.ts
@panel/src/hooks/useZoomContext.ts
@panel/src/types/messages.ts
@panel/src/components/PanelLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickActionButton and QuickActions components</name>
  <files>panel/src/components/QuickActionButton.tsx, panel/src/components/QuickActions.tsx</files>
  <action>
    **QuickActionButton.tsx:**
    - Props: `{ action: QuickActionType; label: string; icon: React.ComponentType<{ className?: string }>; sendAction: (msg: PanelMessage) => void; disabled: boolean }`
    - Internal loading state (useState, default false)
    - onClick handler:
      1. If disabled, return early
      2. Set loading to true
      3. Call `sendAction({ type: 'quick_action', action })`
      4. Set a 5-second setTimeout to clear loading (fallback -- in the future, engine will send an ack that clears this)
    - Render: button with icon + label, flex items-center gap-2
    - Styling: `px-3 py-2 text-xs rounded-lg transition-colors bg-zinc-800 text-zinc-300 hover:bg-zinc-700 disabled:opacity-50 disabled:cursor-not-allowed`
    - When loading: replace icon with Loader2 from lucide-react with `animate-spin`
    - Button disabled when `disabled || loading`
    - Import QuickActionType, PanelMessage from types/messages
    - Import Loader2 from lucide-react

    **QuickActions.tsx:**
    - Props: `{ sendAction: (msg: PanelMessage) => void; disabled: boolean }`
    - Define the 5 quick actions array:
      ```
      { type: 'delegate',      label: 'Delegate Task',   icon: Users }
      { type: 'create_issue',  label: 'Create Proposal', icon: FileText }
      { type: 'research',      label: 'Research This',    icon: Search }
      { type: 'draft_email',   label: 'Draft Email',     icon: Mail }
      { type: 'check_domain',  label: 'Check Domain',    icon: Globe }
      ```
    - Import icons from lucide-react: Users, FileText, Search, Mail, Globe
    - Render in a `grid grid-cols-2 gap-2` container
    - The 5th button (Check Domain) spans full width on its own row -- no special handling needed, grid-cols-2 naturally leaves it alone on the last row
    - Pass sendAction and disabled down to each QuickActionButton
  </action>
  <verify>Run `cd panel && npx tsc --noEmit` to verify both components compile.</verify>
  <done>5 quick action buttons render in a 2-column grid, each with icon, label, loading state, and disabled state when engine is disconnected.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App.tsx to wire PanelLayout + QuickActions + useZoomContext</name>
  <files>panel/src/App.tsx</files>
  <action>
    Rewrite App.tsx to use the new component architecture:

    1. Import `useZoomContext` from `./hooks/useZoomContext`
    2. Import `useEngine` from `./hooks/useEngine`
    3. Import `PanelLayout` from `./components/PanelLayout`
    4. Import `QuickActions` from `./components/QuickActions`

    The new App component should:
    1. Call `useZoomContext()` to get `{ zoomStatus, meetingContext }`
    2. Call `useEngine()` to get `{ connected, connecting, sendAction }`
    3. Render `PanelLayout` with props:
       - `zoomStatus`
       - `engineConnected={connected}`
       - `engineConnecting={connecting}`
       - `quickActions` slot: render `<QuickActions sendAction={sendAction} disabled={!connected} />` -- PanelLayout needs to accept a `footer` prop (React.ReactNode) for this
    4. Keep the debug meetingContext details section at the bottom (inside PanelLayout or after it -- best to keep it as a prop or just render conditionally outside PanelLayout during development)

    **Critical: Update PanelLayout to accept a `footer` prop:**
    Since Plan 02 created PanelLayout without QuickActions (by design), this task must also update PanelLayout.tsx to:
    - Add `footer?: React.ReactNode` to its props
    - Render footer in a `footer.flex-shrink-0.px-3.py-2.border-t.border-zinc-800` wrapper if provided

    The old App.tsx content (inline status dots, flat task list, flat agent grid) should be entirely replaced. All that rendering now lives in PanelLayout and its children.

    After rewriting, run `cd panel && npm run build` to verify the production build succeeds.
  </action>
  <verify>
    Run `cd panel && npm run build` -- must complete with zero errors and produce dist/ output.
    Run `cd panel && npx tsc --noEmit` -- must pass with zero type errors.
  </verify>
  <done>App.tsx is a thin root component (~30 lines) that wires useZoomContext + useEngine into PanelLayout + QuickActions. Production build succeeds. No flat inline rendering remains in App.tsx.</done>
</task>

</tasks>

<verification>
1. `cd panel && npm run build` produces dist/ with zero errors
2. `cd panel && npx tsc --noEmit` passes
3. App.tsx imports and renders PanelLayout and QuickActions
4. QuickActions sends typed messages via sendAction from useEngine
5. All 5 quick actions present: delegate, create_issue, research, draft_email, check_domain
6. No legacy inline rendering remains in App.tsx
</verification>

<success_criteria>
- Panel builds for production successfully
- App.tsx is a clean composition root (~30 lines)
- Quick action buttons are visible in the panel footer
- Buttons are disabled when engine WebSocket is disconnected
- Clicking a button sends the correct message type via WebSocket
- Loading state shows spinner and clears after 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-zoom-panel/04-03-SUMMARY.md`
</output>
