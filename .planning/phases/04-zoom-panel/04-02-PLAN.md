---
phase: 04-zoom-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - panel/src/components/ui/StatusBadge.tsx
  - panel/src/components/ui/CollapsibleSection.tsx
  - panel/src/components/ConnectionStatus.tsx
  - panel/src/components/TaskItem.tsx
  - panel/src/components/TaskFeed.tsx
  - panel/src/components/CompletedItems.tsx
  - panel/src/components/DecisionLog.tsx
  - panel/src/components/AgentStatusGrid.tsx
  - panel/src/components/PanelLayout.tsx
autonomous: true

must_haves:
  truths:
    - "Panel shows active tasks with color-coded status badges (pending=zinc, running=blue pulse, completed=green, failed=red)"
    - "Completed tasks are in a separate collapsible section"
    - "Decisions and notes are displayed in a log section"
    - "Agent status grid shows all 4 agents with busy/idle/error state"
    - "Panel layout uses full-height flex with scrollable main area"
    - "All sections are independently collapsible to maximize narrow sidebar space"
  artifacts:
    - path: "panel/src/components/ui/StatusBadge.tsx"
      provides: "Reusable status indicator with color map"
      exports: ["StatusBadge"]
    - path: "panel/src/components/ui/CollapsibleSection.tsx"
      provides: "Accordion section with count badge"
      exports: ["CollapsibleSection"]
    - path: "panel/src/components/PanelLayout.tsx"
      provides: "Root layout composing all sections"
      exports: ["PanelLayout"]
    - path: "panel/src/components/TaskFeed.tsx"
      provides: "Active task list"
      exports: ["TaskFeed"]
    - path: "panel/src/components/CompletedItems.tsx"
      provides: "Completed task list with results"
      exports: ["CompletedItems"]
    - path: "panel/src/components/DecisionLog.tsx"
      provides: "Decisions and notes section"
      exports: ["DecisionLog"]
    - path: "panel/src/components/AgentStatusGrid.tsx"
      provides: "Agent status indicators"
      exports: ["AgentStatusGrid"]
    - path: "panel/src/components/ConnectionStatus.tsx"
      provides: "Zoom + Engine connection indicators"
      exports: ["ConnectionStatus"]
  key_links:
    - from: "panel/src/components/PanelLayout.tsx"
      to: "panel/src/stores/meetingStore.ts"
      via: "useMeetingStore selector for tasks and agents"
      pattern: "useMeetingStore"
    - from: "panel/src/components/TaskFeed.tsx"
      to: "panel/src/types/messages.ts"
      via: "MeetingTask type for props"
      pattern: "MeetingTask"
    - from: "panel/src/components/AgentStatusGrid.tsx"
      to: "panel/src/types/messages.ts"
      via: "AgentStatus type for props"
      pattern: "AgentStatus"
---

<objective>
Build the complete panel UI component architecture: reusable primitives (StatusBadge, CollapsibleSection), feature components (TaskFeed, CompletedItems, DecisionLog, AgentStatusGrid, ConnectionStatus), and the root PanelLayout.

Purpose: Replace the flat App.tsx rendering with a proper component hierarchy designed for Zoom's narrow (~280px) sidebar. Collapsible sections maximize usable space. Status badges provide consistent visual language across all task and agent displays.

Output: 9 new component files forming the complete panel UI
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-zoom-panel/04-RESEARCH.md
@panel/src/App.tsx
@panel/src/stores/meetingStore.ts
@panel/src/types/messages.ts
@panel/src/hooks/useEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI primitives (StatusBadge + CollapsibleSection)</name>
  <files>panel/src/components/ui/StatusBadge.tsx, panel/src/components/ui/CollapsibleSection.tsx</files>
  <action>
    Create `panel/src/components/ui/` directory and two reusable components:

    **StatusBadge.tsx:**
    - Props: `{ status: TaskStatus }` (import TaskStatus from types/messages)
    - Render a colored dot (w-2 h-2 rounded-full) + text label
    - Color map:
      - pending: `bg-zinc-500`, label "Pending"
      - running: `bg-blue-500 animate-pulse`, label "Running"
      - completed: `bg-green-500`, label "Done"
      - failed: `bg-red-500`, label "Failed"
    - Export named: `StatusBadge`
    - Text should be `text-xs text-zinc-400`

    **CollapsibleSection.tsx:**
    - Props: `{ title: string; count?: number; defaultOpen?: boolean; children: React.ReactNode }`
    - Renders a button header with title on left, count badge + chevron on right
    - ChevronDown icon from lucide-react, rotates 180deg when open
    - Count badge: only show if count > 0, rendered as `bg-zinc-700 text-zinc-300 text-xs px-1.5 py-0.5 rounded-full`
    - Section has `border-b border-zinc-800` divider
    - Header button: `w-full flex items-center justify-between px-3 py-2 text-sm font-medium text-zinc-300 hover:bg-zinc-800/50`
    - Content area: `px-3 pb-3`, only rendered when open
    - Use React useState for open/closed state
    - Export named: `CollapsibleSection`

    Do NOT use fixed pixel widths anywhere -- all components must be fluid (w-full, relative sizing).
  </action>
  <verify>Run `cd panel && npx tsc --noEmit` to verify both components compile.</verify>
  <done>StatusBadge renders color-coded status dots. CollapsibleSection toggles open/closed with chevron animation and optional count badge. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create feature components and PanelLayout</name>
  <files>
    panel/src/components/ConnectionStatus.tsx,
    panel/src/components/TaskItem.tsx,
    panel/src/components/TaskFeed.tsx,
    panel/src/components/CompletedItems.tsx,
    panel/src/components/DecisionLog.tsx,
    panel/src/components/AgentStatusGrid.tsx,
    panel/src/components/PanelLayout.tsx
  </files>
  <action>
    Create 7 components. All use Tailwind classes, no fixed pixel widths, dark theme (zinc-950 bg, zinc-100 text).

    **ConnectionStatus.tsx:**
    - Props: `{ zoomStatus: 'connecting' | 'connected' | 'error' | 'standalone'; engineConnected: boolean; engineConnecting: boolean }`
    - Renders the "Meeting Copilot" title (text-lg font-semibold text-white) with two connection indicators below:
      - Zoom status dot + label (green=connected, yellow=standalone, red=error, zinc pulse=connecting)
      - Engine status dot + label (green=connected, zinc pulse=connecting, red=offline)
    - Indicators in a flex row with gap-4
    - Extract logic from current App.tsx lines 37-69

    **TaskItem.tsx:**
    - Props: `{ task: MeetingTask }` (import from types/messages)
    - Renders single task row: StatusBadge on left, task title, optional result/error text below
    - If task.result exists: show in text-xs text-zinc-500 below title
    - If task.error exists: show in text-xs text-red-400 below title
    - Compact: no padding larger than p-2

    **TaskFeed.tsx:**
    - Props: `{ tasks: MeetingTask[] }`
    - If empty: show "No active tasks" in text-sm text-zinc-500
    - Otherwise: render TaskItem for each task in a `space-y-1` list
    - Filter: only show tasks where status is NOT 'completed' (active tasks only)

    **CompletedItems.tsx:**
    - Props: `{ tasks: MeetingTask[] }`
    - If empty: show "No completed tasks yet" in text-sm text-zinc-500
    - Otherwise: render TaskItem for each completed task
    - Show most recent first (reverse order by completed_at)

    **DecisionLog.tsx:**
    - Props: `{ decisions: Array<{ id: string; text: string; timestamp: string }> }`
    - If empty: show "No decisions recorded" in text-sm text-zinc-500
    - Otherwise: render each decision with timestamp (text-xs text-zinc-600) and text (text-sm text-zinc-300)
    - This section is a placeholder for Phase 5 meeting intelligence -- the structure exists now, data will flow later
    - For now, the store doesn't have decisions yet, so PanelLayout will pass an empty array

    **AgentStatusGrid.tsx:**
    - Props: `{ agents: AgentStatus[] }` (import from types/messages)
    - Render a 2-column grid (grid-cols-2 gap-2)
    - Each agent shows: colored dot (idle=zinc-600, busy=blue-500 pulse, error=yellow-500) + agent name + current task or status text
    - Agent name: text-sm text-zinc-300
    - Status/task text: text-xs text-zinc-500

    **PanelLayout.tsx:**
    - Import and compose all components
    - Use useMeetingStore to get meetingState
    - Use useEngine for connected/connecting status (do NOT call useEngine here -- accept props instead to avoid double WebSocket connections)
    - Actually: PanelLayout should accept props: `{ zoomStatus, engineConnected, engineConnecting }` -- these come from App.tsx which already calls useEngine and useZoomContext
    - Structure:
      ```
      div.h-screen.flex.flex-col.bg-zinc-950.text-zinc-100
        header.flex-shrink-0.px-3.py-2.border-b.border-zinc-800
          ConnectionStatus (with zoom + engine props)
        main.flex-1.overflow-y-auto
          CollapsibleSection title="Active Tasks" count={activeTasks.length} defaultOpen
            TaskFeed tasks={activeTasks}
          CollapsibleSection title="Completed" count={completedTasks.length} defaultOpen={false}
            CompletedItems tasks={completedTasks}
          CollapsibleSection title="Decisions" count={decisions.length} defaultOpen={false}
            DecisionLog decisions={decisions}
          CollapsibleSection title="Agents" defaultOpen
            AgentStatusGrid agents={agents}
        footer -- reserved for QuickActions (Plan 03), render empty for now
      ```
    - Derive activeTasks and completedTasks from meetingState.tasks using filter
    - Pass empty array for decisions (DecisionLog placeholder)
    - Import useMeetingStore to access meetingState directly
    - Do NOT render QuickActions yet -- just add a comment `{/* QuickActions added in Plan 03 */}` in footer area

    Important: Do NOT modify App.tsx in this task. Plan 03 will wire PanelLayout into App.tsx.
  </action>
  <verify>Run `cd panel && npx tsc --noEmit` to verify all 7 components compile with correct type imports.</verify>
  <done>All 7 components exist, compile cleanly, use MeetingTask and AgentStatus types from messages.ts, and PanelLayout composes them into the full sidebar layout with collapsible sections.</done>
</task>

</tasks>

<verification>
1. `ls panel/src/components/` shows: ConnectionStatus.tsx, TaskItem.tsx, TaskFeed.tsx, CompletedItems.tsx, DecisionLog.tsx, AgentStatusGrid.tsx, PanelLayout.tsx
2. `ls panel/src/components/ui/` shows: StatusBadge.tsx, CollapsibleSection.tsx
3. `cd panel && npx tsc --noEmit` passes with zero errors
4. No fixed pixel widths in any component (grep for `w-[` should return nothing in these files)
</verification>

<success_criteria>
- 9 new component files created
- StatusBadge covers all 4 TaskStatus values with correct colors
- CollapsibleSection toggles with chevron animation
- PanelLayout uses full-height flex layout with scrollable main
- All components use fluid widths (no fixed pixels)
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-zoom-panel/04-02-SUMMARY.md`
</output>
