---
phase: 02-context-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/context/profiles.py
autonomous: true

must_haves:
  truths:
    - "Given an attendee email or company name, matching client profile loads from filesystem"
    - "Profile includes communication style, contact info, preferences, and relationship context"
    - "Missing profiles return None (no crash)"
  artifacts:
    - path: "engine/context/profiles.py"
      provides: "Client profile loader from Obsidian vault and client-profiles directory"
      exports: ["load_client_profile", "ClientProfile"]
  key_links:
    - from: "engine/context/profiles.py"
      to: "/opt/agency-workspace/client-profiles/"
      via: "filesystem glob + markdown parsing"
      pattern: "client-profiles.*\\.md"
    - from: "engine/context/profiles.py"
      to: "/opt/agency-workspace/obsidian-vault/Contacts/"
      via: "filesystem glob + frontmatter parsing"
      pattern: "obsidian-vault.*Contacts"
---

<objective>
Build the client profile loader that reads client communication profiles and contact profiles from the local filesystem.

Purpose: Load rich context about WHO the attendee is — communication style, preferences, relationship history — so the copilot can adjust its behavior and provide context to Julian.

Output: `engine/context/profiles.py` with async function to find and parse client profiles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@engine/models.py
@engine/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client profile loader</name>
  <files>engine/context/profiles.py</files>
  <action>
    Create `engine/context/profiles.py` with:

    **Constants:**
    ```python
    CLIENT_PROFILES_DIR = "/opt/agency-workspace/client-profiles"
    OBSIDIAN_CONTACTS_DIR = "/opt/agency-workspace/obsidian-vault/Contacts"
    ```

    **Pydantic model:**
    ```python
    class ClientProfile(BaseModel):
        slug: str  # filename without .md
        name: str
        email: str | None = None
        company: str | None = None
        role: str | None = None
        relationship: str | None = None  # "Client", "Partner", etc.
        communication_style: str | None = None
        formality: str | None = None
        status: str | None = None  # "active", etc.
        raw_content: str = ""  # full markdown for LLM context
        source: str = "client_profiles"  # or "obsidian_contacts"
    ```

    **Function `load_client_profile(email: str | None = None, company: str | None = None) -> ClientProfile | None`:**

    1. Search strategy — try TWO locations:

       **Location 1: `/opt/agency-workspace/client-profiles/`**
       - Glob for all `*.md` files (excluding README.md, templates/, CLIENT-PORTAL-V2-PLAN.md)
       - Read each file, parse YAML frontmatter (between `---` markers)
       - Match by: email in frontmatter OR in file content (case-insensitive), OR company/client name in frontmatter `client` field matching company param
       - Frontmatter fields: `client`, `slug`, `status`, `updated`
       - Content has structured sections: Contact, Communication Style, Language Profile

       **Location 2: `/opt/agency-workspace/obsidian-vault/Contacts/`**
       - Glob for all `*.md` files
       - Read each file, parse YAML frontmatter
       - Match by: `email` field in frontmatter matches email param (case-insensitive), OR `company` field matches company param
       - Frontmatter fields: `name`, `email`, `company`, `role`, `relationship`, `is_client`, `communication_style`, `formality`

    2. Parse frontmatter manually (do NOT add a yaml dependency — just split on `---` and use a simple key:value parser, or add `pyyaml` to requirements.txt since it's a stdlib-adjacent package).

       Actually: add `pyyaml` to requirements.txt. It's worth it for reliable YAML frontmatter parsing. Use `yaml.safe_load()`.

    3. Return the first match found. Priority: client-profiles dir first (richer data), then Obsidian contacts.

    4. If no match, return None.

    5. Include the full raw markdown content in `raw_content` field (trimmed to first 2000 chars to keep context manageable). This gets passed to the LLM as meeting context.

    6. Log at INFO: "Loaded client profile for {name} from {source}" or "No client profile found for email={email} company={company}"

    **Add `if __name__ == "__main__"` block:**
    - Takes email from sys.argv[1] or defaults to "brendan@acrepartner.com"
    - Also accepts --company flag
    - Calls `asyncio.run(load_client_profile(email=email))`
    - Prints result as JSON (or "None found")

    Important: This is filesystem I/O, not network. Use `asyncio.to_thread` for file reads to avoid blocking the event loop, or just use sync reads (acceptable for a handful of small files). Keep it simple.
  </action>
  <verify>
    - `cd /opt/agency-workspace/meeting-copilot/engine && python -c "from context.profiles import load_client_profile, ClientProfile; print('OK')"`
    - `cd /opt/agency-workspace/meeting-copilot/engine && python -m context.profiles brendan@acrepartner.com` returns acre-partner profile
  </verify>
  <done>
    - `load_client_profile(email="brendan@acrepartner.com")` returns ACRE Partner profile with communication style data
    - `load_client_profile(company="Axel Towing")` returns matching profile
    - Unknown email/company returns None without crashing
    - CTX-05 satisfied: client profile loads from Obsidian vault / client-profiles
  </done>
</task>

</tasks>

<verification>
- `engine/context/profiles.py` exists and exports `load_client_profile`, `ClientProfile`
- Import test passes
- Running with known email returns populated ClientProfile
- Running with unknown email returns None gracefully
- `pyyaml` added to requirements.txt
</verification>

<success_criteria>
- CTX-05 satisfied: client profile loads if one exists
- Profile includes communication style, relationship context, and raw markdown for LLM context
- Graceful None return for unknown attendees
</success_criteria>

<output>
After completion, create `.planning/phases/02-context-engine/02-04-SUMMARY.md`
</output>
