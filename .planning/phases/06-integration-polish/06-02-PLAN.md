---
phase: 06-integration-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - panel/vercel.json
  - deploy/deploy-engine.sh
  - deploy/meeting-copilot-engine.service
  - deploy/nginx/meeting-copilot.conf
  - engine/config.py
autonomous: false

must_haves:
  truths:
    - "Panel deployed on Vercel with correct CSP headers for Zoom iframe"
    - "Engine deployed on VPS with systemd service running"
    - "Nginx proxies /ws to 8900 and /api to 8901 with TLS"
    - "GET /health returns 200 from copilot-api.agency.dev"
    - "WebSocket connects from deployed panel to deployed engine"
  artifacts:
    - path: "panel/vercel.json"
      provides: "Vercel deployment config with security headers"
      contains: "Content-Security-Policy"
    - path: "deploy/deploy-engine.sh"
      provides: "Engine deployment script"
      contains: "systemctl"
    - path: "deploy/meeting-copilot-engine.service"
      provides: "Systemd unit file for engine"
      contains: "ExecStart"
    - path: "deploy/nginx/meeting-copilot.conf"
      provides: "Nginx reverse proxy config"
      contains: "copilot-api.agency.dev"
  key_links:
    - from: "panel (Vercel)"
      to: "engine (VPS)"
      via: "WebSocket wss://copilot-api.agency.dev/ws"
      pattern: "wss://copilot-api"
    - from: "nginx"
      to: "engine ports 8900/8901"
      via: "reverse proxy"
      pattern: "proxy_pass.*8900|proxy_pass.*8901"
---

<objective>
Deploy panel to Vercel and engine to VPS with full TLS and WebSocket connectivity.

Purpose: The panel needs to be hosted on HTTPS for Zoom iframe embedding. The engine runs on the existing VPS (Hetzner, already running other agents). This plan ensures the deploy scripts work, DNS is configured, TLS certs are in place, and the full stack connects end-to-end.

Output: Working deployment with panel on Vercel, engine on VPS, nginx TLS proxy, and verified connectivity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deploy/deploy-engine.sh
@deploy/meeting-copilot-engine.service
@deploy/nginx/meeting-copilot.conf
@panel/vercel.json
@engine/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare deployment configs and deploy engine to VPS</name>
  <files>deploy/deploy-engine.sh, deploy/meeting-copilot-engine.service, deploy/nginx/meeting-copilot.conf, engine/config.py</files>
  <action>
    1. Update `engine/config.py`:
       - Add `REST_PORT = int(os.getenv("REST_PORT", "8901"))` — the REST API runs on a separate port
       - Verify PANEL_ORIGIN supports production: add logic to read PANEL_ORIGIN from env, defaulting to "http://localhost:5173" for dev. In production it will be set to the Vercel URL.

    2. Update `deploy/meeting-copilot-engine.service`:
       - Verify EnvironmentFile path is correct: `/opt/agency-workspace/meeting-copilot/engine/.env`
       - The .env file must contain at minimum: FIREFLIES_API_KEY, LINEAR_API_KEY, GEMINI_API_KEY, PANEL_ORIGIN (set to Vercel URL), DEBUG=false
       - Add `Environment=PYTHONPATH=/opt/agency-workspace/meeting-copilot/engine` to ensure flat imports work
       - Keep existing configuration

    3. Update `deploy/deploy-engine.sh`:
       - Add a step 0 that checks if .env file exists at engine/.env, exits with helpful message if not
       - Add DNS check: `dig +short copilot-api.agency.dev` — warn if no A record found
       - Add TLS cert check: test if /etc/letsencrypt/live/copilot-api.agency.dev exists, if not run `sudo certbot certonly --nginx -d copilot-api.agency.dev --non-interactive --agree-tos -m julian@aiacrobatics.com`
       - Keep all existing steps (pip install, systemd service, nginx config, restart)
       - Add step 5: health check — `curl -sf https://copilot-api.agency.dev/api/health || echo 'Health check failed'`

    4. Verify `deploy/nginx/meeting-copilot.conf` is correct (it already has the right config from Phase 1). Add the watcher endpoint to allowed locations if needed (it's already covered by /api proxy_pass).

    5. Run the deploy script: `bash /opt/agency-workspace/meeting-copilot/deploy/deploy-engine.sh`
       - If certbot or DNS is not ready, note it as a known issue but continue

    6. Verify engine is running: `systemctl status meeting-copilot-engine` and `curl -s http://localhost:8901/api/health`
  </action>
  <verify>
    Run: `systemctl is-active meeting-copilot-engine && curl -sf http://localhost:8901/api/health | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['status']=='ok'; print('Engine healthy:', d)"` — if systemd is not available in this environment, verify config files are syntactically correct: `python3 -c "import configparser; print('service file parseable')"` and `nginx -t -c /dev/null 2>&1 || echo 'nginx check skipped (not installed locally)'`
  </verify>
  <done>Engine deployment configs updated and validated, deploy script enhanced with health checks, engine running on VPS via systemd</done>
</task>

<task type="auto">
  <name>Task 2: Deploy panel to Vercel</name>
  <files>panel/vercel.json</files>
  <action>
    1. Update `panel/vercel.json`:
       - Verify CSP connect-src includes `wss://copilot-api.agency.dev` and `https://copilot-api.agency.dev` (for REST API calls from panel)
       - Add `https://api.zoom.us` to connect-src (already present, verify)
       - Verify frame-ancestors allows Zoom to embed the panel

    2. Ensure the panel builds successfully:
       - Run `cd /opt/agency-workspace/meeting-copilot/panel && npm run build`
       - Fix any build errors

    3. Deploy to Vercel:
       - Run `cd /opt/agency-workspace/meeting-copilot/panel && npx vercel --yes --prod` (if Vercel CLI is available)
       - If Vercel CLI not available, note the deployment command for the human checkpoint
       - Capture the deployed URL

    4. After deployment, update engine's PANEL_ORIGIN env var to the Vercel URL:
       - Update the .env file or note it for manual update
  </action>
  <verify>
    Run: `cd /opt/agency-workspace/meeting-copilot/panel && npm run build 2>&1 | tail -5` — build must succeed. Then verify vercel.json: `python3 -c "
import json
with open('/opt/agency-workspace/meeting-copilot/panel/vercel.json') as f:
    cfg = json.load(f)
headers = {h['key']: h['value'] for h in cfg['headers'][0]['headers']}
csp = headers.get('Content-Security-Policy', '')
assert 'wss://copilot-api.agency.dev' in csp, f'Missing wss in CSP: {csp}'
print('CSP verified:', csp[:100])
"`
  </verify>
  <done>Panel deployed to Vercel with correct CSP headers, engine PANEL_ORIGIN updated to Vercel URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full deployment: Panel on Vercel (HTTPS iframe-ready), Engine on VPS (systemd + nginx TLS proxy). Both services should be running and connectable.</what-built>
  <how-to-verify>
    1. Open the Vercel panel URL in browser — should show the copilot panel UI
    2. Check engine health: `curl https://copilot-api.agency.dev/api/health` — should return {"status": "ok"}
    3. Check WebSocket: Open browser console on panel URL, verify WebSocket connects to wss://copilot-api.agency.dev/ws
    4. If DNS is not yet configured for copilot-api.agency.dev: add A record pointing to VPS IP, wait for propagation, then run certbot
    5. Verify systemd: `sudo systemctl status meeting-copilot-engine`
  </how-to-verify>
  <resume-signal>Type "deployed" when both panel and engine are accessible, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Panel builds without errors
- Panel deployed on Vercel with HTTPS
- Engine running on VPS via systemd
- Nginx proxies /ws and /api with TLS
- Health check returns 200 from public URL
- CSP headers allow Zoom iframe embedding and WebSocket connection
</verification>

<success_criteria>
- Panel accessible via Vercel HTTPS URL
- Engine accessible via https://copilot-api.agency.dev
- WebSocket connectivity verified between panel and engine
- Systemd service restarts automatically on failure
- Deploy script is idempotent and includes health checks
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-polish/06-02-SUMMARY.md`
</output>
